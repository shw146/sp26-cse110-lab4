1. values added: 20
2. final result: 20
3. Variables declared using "var" are not restrained to their own individual scope, meaning that apart from a function scope, var type variables are accessible outside of their if/while/for blocks, making it easy for us to accidentally use incorrect values in our code.
4. values added: 20
5. Line 13 will return an error because the if block contains the result variable in its own scope, making it inaccessible to line 13
6. The code will give an error because the const variable type cannot be changed how the code attempts to do so in line 7
7. Because result keeps its own scope, line 13 has no declared variable reuslt to pull its value from
